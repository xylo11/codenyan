<%= form_with(model: [@commentable, Comment.new], remote: true, html: { class: "new_comment" }) do |form| %>
  <div class="field">
    <%= render 'layouts/ckeditor', form: form, field_name: :content %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>