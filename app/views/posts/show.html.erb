<h2><%= link_to @post.park.name, @post.park %></h2>
<p>Posted by <%= link_to @post.user.username, @post.user %> <%= time_ago_in_words(@post.created_at) %> ago</p>
<h1><%= @post.title %></h1>
<p><%= @post.content %></p>

<%# The comment form, to be implemented later %>
<div class="comment-form">
  <h3>Post a Comment</h3>
  <!-- Form goes here -->
</div>

<%# The comments list, to be implemented later %>
<div class="comments-list">
  <h3>Comments</h3>
  <!-- Comments go here -->
</div>

<%= link_to 'Edit', edit_park_path(@post.park) %> |
<%= link_to 'Destroy', @post, method: :delete, data: { confirm: 'Are you sure?' } %> |
<%= link_to 'Back', posts_path %>